version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./config:/app/config
      - ./attachments:/app/attachments
      - ./logs:/app/logs
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
